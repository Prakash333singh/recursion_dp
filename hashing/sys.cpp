
#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_map<int, int> mp;

    // mp.insert(make_pair(3, 100));

    // if (mp.find(3) != mp.end())
    // {
    //     cout << "not found\n";
    // }

    // cout << mp[3] << endl;

    // for (int i = 0; i < 5; i++)
    // {
    //     mp[i] = i + 1;
    // }

    // for (int i = 0; i < 5; i++)
    // {
    //     cout << mp[i] << " ";
    // }

        return 0;
}